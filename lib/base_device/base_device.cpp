#include "base_device.h"
/*
void BasePubDevice::nonblocking_publish_loop()
{
    unsigned long current_millis = millis();
    if (current_millis - this->previous_millis >= this->publish_interval)
    {
        this->publish_once();
        this->previous_millis = current_millis;
    }
}
*/